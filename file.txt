Build a new feature for the AI Resume + Job Match Coach application.

Requirement:
Create a professional landing page with a resume upload feature.

Feature Details:
1. Design a modern, clean, responsive landing page.
2. Main headline should clearly explain the product helps users improve resumes and match jobs using AI.
3. Add a prominent CTA section with:
   - Upload Resume button
   - Support for PDF and DOCX files only
4. Show drag-and-drop upload area along with manual file select option.
5. Display accepted file formats: PDF, DOCX.
6. Validate file type before upload.
7. Show upload progress indicator.
8. Show success message after upload.
9. Show error message for invalid file type or failed upload.
10. Include sections below hero area:
   - How It Works (3 simple steps)
   - Benefits of using AI Resume Coach
   - Testimonials / trust section
11. Make UI mobile responsive and visually premium.
12. Use clean animations and modern styling.

Project Structure Requirements:
1. Add all frontend/UI code inside the frontend folder.
2. Add all backend/API/upload related code inside the backend folder.
3. Keep components modular and maintain clean folder structure.
4. Separate reusable UI components, pages, services, and utilities properly.

Technical Requirements:
Frontend:
- Next.js + React + Tailwind CSS
- Component-based structure
- Use reusable upload component
- Drag-and-drop upload support
- API integration ready

Backend:
- FastAPI (Python)
- Endpoint: POST /upload-resume
- Accept multipart PDF/DOCX uploads
- Validate file types
- Store uploaded files
- Return success response with file_id and filename
- Handle upload errors properly

Suggested Folder Structure:

frontend/
  app/
  components/
  services/
  utils/

backend/
  routes/
  services/
  uploads/
  main.py

Deliverables:
- Landing page UI
- Resume upload component
- Validation logic
- Backend upload API
- Clean structured codebase
- Ready to run locally

Goal:
A user should land on the page and immediately upload their resume in PDF/DOCX format with a smooth and professional experience.

# Save uploaded resume to db
Implement the service logic for an existing upload-resume REST API. This task involves integrating AWS S3 for file storage and PostgreSQL for metadata persistence.

1. Database Schema (PostgreSQL)
Create a table named resume_metadata with the following specification:
id: UUID or String (Primary Key).
user_id: String (Foreing Key Indexed).
s3_key: String (Unique; stores the path/URL returned from S3).
status: Enum with values PENDING, SUCCESS, FAILED.
uploaded_at: Timestamp (Defaults to current time).
size: Float (File size in bytes or KB).

2. Functional Requirements & Logic Flow
Implement a service function that handles the following sequence:
Identity Resolution: Check if a user_id is provided. If the user is unregistered (no ID provided), generate a dummy user_id (e.g., guest_[unique_suffix]) to associate with the resume.
Initial Persistence: Create a record in resume_metadata with the status set to PENDING.

3. S3 Integration: * Accept .pdf and .docx file types.
Upload the file to an S3 bucket.
Capture the returned s3_key or Object URL.

4. Finalize Record:
On Success: Update the database record with the s3_key, the calculated file size, and change the status to SUCCESS.
On Failure: If the S3 upload fails, update the database status to FAILED and log the error.

3. Technical Constraints
Ensure the file size is calculated correctly before or during the upload.
Use a clean, modular approach (e.g., separating the S3 client logic from the database repository logic).
Provide the SQL DDL for the table and the Enum type.

Output Expectations:
SQL migration script for the PostgreSQL table.

# Parse resume
Build a production-grade Resume Parsing System.

## Goal
The current upload-resume API should extracts raw text, uses an LLM to convert resume text into structured JSON, validates the output, and stores parsed data for downstream features.

The parsed output must support:
1. ATS Score
2. Resume strengths
3. Weak sections
4. Missing keywords
5. Formatting suggestions
6. Job Match Score
7. Resume Tailoring
8. Interview Question Generation

---

## Tech Stack

Frontend:
- Next.js
- React
- Tailwind CSS

Backend:
- FastAPI (Python)

Database:
- PostgreSQL

Storage:
- AWS S3

Libraries:
- PyMuPDF (PDF parsing)
- python-docx (DOCX parsing)
- Pydantic
- SQLAlchemy
- Alembic

LLM:
- OpenAI / Claude API

---

## Folder Structure Requirement

Put all frontend code inside:

frontend/

Put all backend code inside:

backend/

I am building a new feature, It should only write UI code for now. Later a new backend API will be integrated to this UI.

Frontend Implementation Details:
Landing Page UI: Add a "Know ATS" action button.

Logic & State:

Enabled: The button becomes active only after a resume has been successfully uploaded.

Disabled: If no resume is present, the button is locked.

User Feedback: Implement a hover tooltip for the disabled state that reads: "Upload resume to know your ATS."

Architecture:

Create a new service file: frontend/services/ats.js (or .ts).

Styling: Ensure the new component strictly adheres to the existing design system and CSS variables for visual consistency.

Backend:
Endpoint: POST /know-ats
Functional Requirements:

Input Validation: The API must accept a mandatory jobDescription string in the request body.

Constraint: Minimum length of 1 character; maximum length of 5,000 characters.
Error Handling: Return a 400 Bad Request if the field is missing or exceeds the character limit.

Data Transformation: Convert the raw Job Description (JD) text into a structured JSON format, utilizing the existing parsing logic established in the upload-resume API.
Database Persistence: Update the parsed_resume table by saving the structured JSON into a new column specifically named parsedJD.
ATS Logic Placeholder: Implement a designated placeholder block where the ATS scoring algorithm will be integrated in the next phase.

Frontend:
Bind the "Know ATS" button to a click handler that sends the Job Description (JD) text to the backend.
Implement a loading state on the button to provide visual feedback during the API request.
Ensure the request is only fired if the jobDescription is within the 1-5,000 character range.

This measures how closely the "vibe" and "domain" of the resume match the JD.

Generate Embeddings: Convert your Resume JSON string and JD JSON string into vectors.

Calculate Cosine Similarity: Use a math library to find the distance between these two vectors.

The Result: A score between 0.0 and 1.0 representing overall semantic alignment.

Part B: The LLM Scorer (Qualitative)
This identifies specific "Hard Matches" and "Gaps."

Prompting: Feed the LLM both JSONs.

Logic: Ask the LLM to perform a "Gap Analysis" (e.g., "The JD requires 3 years of AWS; the resume shows 1 year. Deduct 10 points.")

The Result: A detailed score based on specific constraints and logic.

# ATS logic
**Role & Objective:**
You are an expert AI Backend Engineer. Your task is to build a hybrid ATS (Applicant Tracking System) scoring pipeline that compares a parsed Resume JSON against a parsed Job Description (JD) JSON. 

**System Architecture:**

**Part A: The Vector Engine (Quantitative Analysis)**
1. Implement a local embedding pipeline using the `BGE-M3` model (optimized for CPU/lightweight execution).
2. Convert the Resume JSON string and the JD JSON string into vector embeddings.
3. Calculate the Cosine Similarity between the two vectors to generate a semantic alignment score (0.0 to 1.0).

**Part B: The LLM Evaluation (Qualitative Analysis & Final Audit)**
1. Integrate with `llama3.2:latest` (currently running locally, later migrating to vLLM on RunPod/Lambda Labs).
2. Send the parsed Resume JSON, parsed JD JSON, and the Quantitative Score from Part A to the LLM to act as a Senior Technical Recruiter.
3. The LLM must return a structured JSON response extracting specific insights (schema provided below).

**Database Implementation:**
Create an SQL query and the corresponding ORM model (e.g., SQLAlchemy) for a new table to store the final evaluation records. The table must include:
* `id` (Primary Key, UUID)
* `resume_id` (Foreign Key, UUID)
* `timestamp` (DateTime, default to current time)
* `ats_score` (Integer)
* `strengths` (JSON/Array of strings)
* `weaknesses` (JSON/Array of strings)
* `missing_keywords` (JSON/Array of strings)
* `formatting_feedback` (Text/JSON)
* `match_report` (Text)

Please write the Python code to orchestrate this pipeline, including the embedding generation, the LLM API call, and the database schema.
A wrapper API for this functionality is already created with name "/know-ats".
Add the code here 
# ── PLACEHOLDER: ATS scoring algorithm goes here ──────────────────────
in the @routes/ats.py know_ats function

# Analysis Dashboard
Build a frontend React component for an ATS Analysis Dashboard
Step 1: The Trigger. Create a view with a 'Know ATS' button. 
When clicked, it calls the know-ats API. Wait for the response.
Do not display a 'View Dashboard' navigation button unless the API returns a 200 OK status.

Step 2: The Dashboard Page. Once on the dashboard page, consume the JSON response.
Display the ats_score at the very top as a massive <h1> hero element.
Below it, create a well-organized grid or list layout to display the remaining data fields: match_report, strengths, weaknesses, missing_keywords, and formatting_feedback.

Step 3: Styling & UI Consistency. It is critical that you write the CSS/styling (using Tailwind, styled-components, or standard CSS, depending on my project setup) to perfectly match the existing design system.
Reuse the established color scheme, typography, and spacing conventions of the app. Do not invent a new UI theme; make it look like a native extension of the current application."

# Column addition to the Analysis Report
We are expanding our existing ats_service pipeline. It currently compares a Resume and JD to generate an ats_score, strengths, weaknesses, missing_keywords, formatting_feedback, and match_report, saving them to the ats_evaluations table and showing them on a frontend dashboard.
Please generate the code to add three new metrics to this entire stack: match_percentage (int), matching_skills (array of strings), and experience_fit (string).
Task 1: LLM Prompt & Schema: Update the JSON output schema and the system prompt for the LLM API call to evaluate and return these three new fields.
Task 2: Database Model: Write the SQL migration or ORM code to add these three new columns to the existing ats_evaluations table.
Task 3: Frontend Component: Update the React/Next.js dashboard component to map and display these new fields within the existing UI layout, strictly adhering to the current design system and color schemes.